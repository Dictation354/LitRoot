export const dependencyRepair = {
  node: 'nvm install 24 && nvm use 24',
  paperFetch: 'https://github.com/Dictation354/paper-fetch-skill/releases/latest',
  git: 'sudo apt-get update && sudo apt-get install -y git'
} as const
